<?xml version="1.0" encoding="utf-8"?>
<navigation
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/welcomeFragment">

    <fragment
        android:id="@+id/welcomeFragment"
        android:name="com.example.practica.WelcomeFragment"
        android:label="Bienvenida">

        <action
            android:id="@+id/action_welcomeFragment_to_listaFragment"
            app:destination="@id/listaFragment"

            app:popUpTo="@id/welcomeFragment"
            app:popUpToInclusive="true"/>
    </fragment>

    <fragment
        android:id="@+id/listaFragment"
        android:name="com.example.practica.ListaFragment"
        android:label="Lista">

        <action
            android:id="@+id/action_listaFragment_to_detalleFragment"
            app:destination="@id/detalleFragment"/>
    </fragment>

    <fragment
        android:id="@+id/detalleFragment"
        android:name="com.example.practica.DetalleFragment"
        android:label="Detalle">

        <argument
            android:name="itemId"
            app:argType="integer"
            android:defaultValue="0"/>
    </fragment>

</navigation>